# Hosted runtime secret, configured through Sites environment settings.
# Public-data fine-grained GitHub token; no additional permissions required.
# For local use, prefer npm run dev:github (uses gh sign-in in memory).
GITHUB_TOKEN=
